<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Добавление новой книги</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      border: none;
      outline: none;
    }
    body {
      font-family: sans-serif;
      font-size: 16px;
    }
    .wrapper {
      max-width: 1200px;
      margin: 50px auto;
    }
    .page-title {
      margin-bottom: 30px;
    }

    form {
      width: 100%;
      display: flex;
      flex-direction: column;
    }
    form > div {
      margin-bottom: 20px;
    }
    form label {
      display: block;
      margin-bottom: 5px;
    }
    input[type="text"] {
      width: 400px;
      font-size: 16px;
      padding: 10px 15px;
      border: 1px solid #999;
    }
    input[type="text"]:focus,
    input[type="text"]:focus-visible {
      border: 1px solid #333;
    }
    input[type="submit"] {
      width: 150px;
      height: 45px;
      border: none;
      font-size: 18px;
      border: 1px solid #333;
      color: #333;
      cursor: pointer;
    }
  </style>
</head>
<body>
  <main>
    <div class="wrapper">
      <h1 class="page-title">Добавление новой книги</h1>
      <form action="/book/create" method="post">
        <div>
          <label for="title">Название книги</label>
          <input type="text" name="title" id="title" />
        </div>
        <div>
          <label for="description">Описание книги</label>
          <input type="text" name="description" id="description" value="<%= book.description %>" />
        </div>
        <div>
          <label for="author">Автор книги</label>
          <input type="text" name="author" id="author" value="<%= book.author %>" />
        </div>
        <div>
          <label for="fileCover">Обложка книги</label>
          <input type="text" name="fileCover" id="fileCover" value="<%= book.fileCover %>" />
        </div>
        <div>
          <label for="fileName">Файл книги</label>
          <input type="text" name="fileName" id="fileName" value="<%= book.fileName %>" />
        </div>
        <div>
          <input type="submit" value="Добавить" />
        </div>
      </form>
    </div>
  </main>
</body>
</html>